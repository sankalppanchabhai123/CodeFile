package LinkedList.Inbuild_LinkedList;
// import java.util.*;
import java.util.LinkedList;
public class basic {
    public static void main(String args[]){
        // creation
        LinkedList<Integer> ll=new LinkedList<>();
        ll.addLast(2);
        ll.addLast(3);
        ll.addFirst(1);
        ll.set(1, 5);
        System.out.println(ll);
    }
}
// contest provider, dreeling ,we have do the internship in the good compony